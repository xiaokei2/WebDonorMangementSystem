%li
  = gravatar_for user, size: 50
  = link_to user.name, user
  - if current_user.admin? && !current_user?(user)
    - if !user.admin?
      = link_to "[Make admin] ", :action => :make_admin, :user_id => user.id
      = link_to "[Delete]", user, method: :delete, data: { confirm: "Are you sure?" }